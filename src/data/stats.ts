/**
 * Números animados da seção "Números" (home).
 *
 * ⚠️ `clientsServed` e `averageRating` são PLACEHOLDERS — troque pelos
 * valores reais da barbearia assim que tiver. `foundingYear` já é real
 * (usado pra calcular "anos de tradição" automaticamente, sem precisar
 * atualizar todo ano).
 */
export const STATS_CONFIG = {
  foundingYear: 2015,
  /** PLACEHOLDER — troque pelo número real de clientes atendidos. */
  clientsServed: 5000,
  /** PLACEHOLDER — troque pela avaliação média real (Google, Instagram, etc.). */
  averageRating: 4.9,
};
